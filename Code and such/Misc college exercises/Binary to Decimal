inputVar = float(input("Please enter a decimal number: "))
big, small = divmod(inputVar, 1)

big = int(big)
firstHalf = []
secondHalf = []

while big > 1:
    inputTuple = divmod(big, 2)
    firstHalf.insert(0, inputTuple[1])
    big = inputTuple[0]
firstHalf.insert(0, 1)

while small 
    